#define TEST_ENUM(V)                                                          \
    V(async_write)                                                            \
    V(both_eof)                                                               \
    V(eof)                                                                    \
    V(partial_write)                                                          \
    V(sync_write)
