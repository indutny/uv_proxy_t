#define TEST_ENUM(V)                                                          \
    V(async_write)                                                            \
    V(eof)                                                                    \
    V(partial_write)                                                          \
    V(sync_write)
